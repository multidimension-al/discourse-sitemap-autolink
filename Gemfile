# frozen_string_literal: true

# Linting only — the plugin itself has no runtime dependencies beyond
# Discourse. CI installs this before running rubocop.
source "https://rubygems.org"

gem "rubocop-discourse"
